Implemented mobile responsive CSS for CSM statistics page at `src/main/resources/templates/design/consultation-stats.html` on 2026-05-04. Added scoped media queries for `.app[data-screen-label="Consultation Stats"]` at 768px and 480px. 768px: hides page-level horizontal overflow, reduces main padding, wraps page header/filter/section headings, gives filter fields responsive widths, hides filter spacer, enforces 44px export min-height, makes `.cs-table-wrap` horizontally scrollable with `-webkit-overflow-scrolling: touch`, sets table min-width 760px, and reduces chart heights. 480px: compact title/subtitle, one-column filter, 44px/16px selects, full-width export button with active feedback, compact section cards/head/body, wider flexible section number badges for values like `1-1` and `차트`, table min-width 680px with smaller cell padding, compact chart heights, and compact empty state. PC styles above 768px were left unchanged except existing pre-existing 1100px split breakpoint.